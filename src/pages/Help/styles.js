import styled from "styled-components";

import {
  blockElement
} from "../../styles/helpers";

export const Container = styled.div`
  ${blockElement}

  width: 100%;
  margin: 0 auto;
  cursor: pointer;
`;